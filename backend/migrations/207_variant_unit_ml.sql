-- 207: обʼєм упаковки (мл/г на одну пляшку/тюбик) — щоб грамовий облік читався як пляшки.
-- Продукт, що продається за грам/мл (price_per_gram), тримає склад у базовій одиниці (мл/г),
-- а unit_ml дає перерахунок у пляшки для відображення й для приходу накладною (×unit_ml).
ALTER TABLE product_variants ADD COLUMN IF NOT EXISTS unit_ml NUMERIC(10,2);
