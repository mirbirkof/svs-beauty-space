-- 279: персональний повний набір прав користувача (Босс 19.07).
-- Якщо NULL — діють права ролі (+ extra_permissions), як раніше.
-- Якщо масив — це ТОЧНИЙ ефективний набір прав користувача (перекриває роль).
-- Дозволяє власнику вмикати/вимикати БУДЬ-ЯКЕ право адміну прямо з профілю.
ALTER TABLE users ADD COLUMN IF NOT EXISTS permissions_override jsonb DEFAULT NULL;
