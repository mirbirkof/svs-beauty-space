-- ═══════════════════════════════════════════════════════
-- 196_require_open_shift_setting.sql — #103: смена кассы НЕобязательна
-- Касса магазина и салона пишут в одни cash_operations; требование
-- открытой смены блокировало проведение услуг/оплат. Теперь это настройка:
--   require_open_shift = 'false' (дефолт) → операции проводятся и без смены
--                                            (shift_id = NULL, колонка nullable с 139)
--   require_open_shift = 'true'           → старое поведение (жёсткая блокировка)
-- Если смена открыта — операция привязывается к ней как раньше.
-- ═══════════════════════════════════════════════════════
INSERT INTO app_settings (key, value) VALUES
  ('require_open_shift', 'false'::jsonb)
ON CONFLICT (key) DO NOTHING;
